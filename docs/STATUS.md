# Blis-Q — Build Status

> Living status board. **Update this whenever a piece of work lands** (merged PR) or a new branch starts. Pair with [docs/ROADMAP.md](ROADMAP.md) (the plan), [docs/API.md](API.md) (the contract), and `CLAUDE.md` (rules + issue tracker).

_Last updated: 2026-07-08 — **In flight: `feat/crisis-contacts-backend`** (P-37 safety-page slice 1). Last merged: #87. Batch merged since the previous update: **P-37 Support & Education** — admin-web Resources CRUD (#81), required-field asterisks across the admin portal (#82), and the **mobile Resources feature** reworked into ONE full-bleed directory screen with **server-side `?search=`** (#83). **P-10a** transient-network logout fix — `refreshSession()` keeps the session on a network/5xx blip instead of forcing a re-login (#84). The **app-wide full-bleed header sweep** — a shared `ScreenHeader` replacing the native stack bar across 6 pushed screens (#85) + `ChatThread` (#86). The **premium glass Home cards** revamp — community rail tiles + event cards + category chips (#87). Earlier the same day: the **CI speedup** (integration tests on a local Postgres 17 container, ~21→~3 min, #79/#80). Sprint 7 (Support & Education + Safe Places + content pillars). **Next up:** emergency/crisis contacts (P-37). Prior "in flight" entry retained below for history —_
_**P-37 Support & Education, slice 2 (admin-web Resources CRUD)** merged (#81) (`feat/admin-resources-web`): a new **`ResourcesPage`** in the admin portal (list + category filter + create/edit drawer + soft-delete) consuming the slice-1 admin API — **frontend-only** (no backend/schema/contract change). Follows the modernized admin design system (`ui.tsx`/`DataTable`/`Drawer`/`Icon`), mirrors `SafePlacesPage` stripped to resources' shape; adds a local mirror of `RESOURCE_CATEGORIES`/`ResourceDTO` to `admin/src/lib/types.ts`, a `book` icon, and a "Materiały" nav item under **Treści**. Gate: `cd admin && npm run build` (admin is excluded from root `check:types` + not built in CI — infra follow-up flagged) + **human browser test**. **Slice 1 (backend) merged (#77):** a new admin-curated **`resources`** content model + read/admin APIs. Additive `resources` table (RLS zero-policy; soft-delete; `created_by_id` set-null on erasure), frozen `RESOURCE_CATEGORIES` (mental_health/legal_rights/community_orgs/education_careers/health_services/housing_support — Article-9-safe content topics), `ResourceDTO {title,category,body,url,featured,createdAt}`; `GET /api/v1/resources (+?category=)` + `/:id` (featured-first, soft-deleted excluded) + admin CRUD (`GET/POST /admin/resources`, `PATCH/DELETE /:id`, requireAdmin + rate-limited + transactional + audited IDs-only). Mirrors the safe-places vertical. **Schema must be applied to TEST+PROD via docs/DEPLOY.md (human) before CI integration passes.** Deferred (P-37): emergency contacts (own table + safety bar), mobile Resources screen, suggest-a-resource pipeline, images. Prior context — recent merges: Communities UI redesign (#70), skeleton loading (#71/#73/#74), Profile redesign (#75), autoslice Codex-gate easing (#72). Earlier: docs reconciliation (`docs/roadmap-deck-reconciliation`): (1) corrected ROADMAP **Sprint 6** — "notifications complete" was inaccurate (only the send pipeline exists; no push-token route / prefs screen / reminder+retention cron → no device receives a push yet); (2) expanded ROADMAP **Sprint 7** to the deck's real pillar-3 + pillar-4 scope and folded the tracker-only items **P-31 (News) · P-32 (Initiatives) · P-33 (profile depth) · P-37 (Support & Education)** into the sprint plan; (3) added a **"Pitch-deck pillar coverage"** map to ROADMAP; (4) synced these phase headers from "Sprint 5/6" to **Sprint 7**. Safe Places P-40 epic to date: backend #60, admin CRUD #61, OSM import #62, list/search #63, save #64, images #65, detail #66, accessibility #67, **UI redesign #68**. Quick-exit/discreet-mode safety is PAUSED (P-17)._

## Current phase

**Sprint 7 — Safe Places + Support/Education + News/Initiatives content (ROADMAP Sprint 7).** Sprints 1–5 ✅ (auth, account/GDPR, communities, posts/moderation, community chat). **Sprint 6 (events) ✅ shipped in full** (#44–#59) — **but the Sprint-6 "notifications" scope did NOT ship complete**: only the `server/notifications.ts` _sending pipeline_ exists (wired best-effort into new posts + new events); there is **no push-token registration route, no notification-preferences screen, no reminder/retention cron**, so no device actually receives a push yet (see Known follow-ups + ROADMAP Sprint 6 status correction, 2026-07-05). **Now Sprint 7:** the Safe Places **P-40 epic** is well underway (backend #60, admin CRUD SP-1 #61, OSM import SP-2 #62, mobile list/search SP-3 #63, save #64, images SP-6a #65, detail #66, accessibility #67, **UI redesign #68**); still open in the epic: map + near-me (SP-4), user submissions (SP-5). **Deck pillars 3 + 4 content — Support & Education (P-37), News (P-31), Initiatives/surveys/volunteering (P-32), profile/networking depth (P-33) — are now scheduled into Sprint 7** (were tracker-only; folded into the roadmap 2026-07-05). Quick-exit + discreet-mode safety is PAUSED pending a client/product decision (P-17).

## Merged to `main`

| PR  | What                                                                                                                                                                                                                                                                                                                                                                                                              |
| --- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| #1  | Scaffold (12 Even Tab files adapted + fresh infra/config)                                                                                                                                                                                                                                                                                                                                                         |
| #2  | `docs/ROADMAP.md` (6-month plan)                                                                                                                                                                                                                                                                                                                                                                                  |
| #3  | `docs/API.md` v1 API contract + `shared/types.ts` + Zod schemas                                                                                                                                                                                                                                                                                                                                                   |
| #4  | `feat/auth-core` — consent-backed signup + login (verification-first); validated with a real-Supabase smoke test                                                                                                                                                                                                                                                                                                  |
| #5  | `fix/env-empty-string` — empty optional env vars treated as unset                                                                                                                                                                                                                                                                                                                                                 |
| #6  | `feat/auth-password-reset` — atomic single-use expiring reset tokens, no enumeration, audit                                                                                                                                                                                                                                                                                                                       |
| #7  | `feat/auth-google` — Google sign-in (Supabase `signInWithIdToken`, Option A); consent on first sign-up, fail-closed cleanup; +`forceExit` CI fix                                                                                                                                                                                                                                                                  |
| #8  | `feat/auth-screens-mobile` — end-to-end mobile auth UI (Polish/LTR), Google consent retry, SecureStore session, push-deregister on logout, jest-expo harness                                                                                                                                                                                                                                                      |
| #9  | `feat/admin-login` — real admin email/password sign-in (`POST /api/admin/login`, server-side `isAdmin` gate, generic 401, session revoke + sanitized logging, audit)                                                                                                                                                                                                                                              |
| #10 | `feat/account-profile` — Sprint-2 slice 1: `GET/PATCH /api/v1/profile`, `POST /account/change-password` (verification-session revoked on every exit), `GET /account/consents`; closes the P-1 "no GET /me" gap                                                                                                                                                                                                    |
| #11 | `feat/account-export` — Sprint-2 slice 2: `GET /api/v1/account/export` (GDPR Art. 20); expanded shape (notif prefs, blocks, reports, subscription), soft-deleted incl., security exclusions documented                                                                                                                                                                                                            |
| #12 | `feat/account-erasure` — Sprint-2 slice 3: `DELETE /api/v1/account` (GDPR Art. 17) anonymisation cascade across every table; **closes P-1/P-2**                                                                                                                                                                                                                                                                   |
| #13 | `feat/communities` — Sprint-3 slice 1: communities create/browse/get/join/leave, creator→admin, last-admin-leave guard (409), audited                                                                                                                                                                                                                                                                             |
| #14 | `feat/block-reports` — Sprint-3 slice 2: blocks (block/unblock/list, idempotent, soft-deleted unavailable) + `POST /reports`; block-only (mute deferred); audited                                                                                                                                                                                                                                                 |
| #15 | `feat/theme-foundation` — Sprint-3 mobile PR 1: light/dark theme (`ThemeContext`, SecureStore-persisted, default dark) + ~20-file `useTheme()` migration + authenticated tab shell; `HomePlaceholder` removed                                                                                                                                                                                                     |
| #16 | `feat/communities-mobile` — Sprint-3 mobile PR 2: communities browse/detail/create + blocked-users under Events tab; login-first redesign + sun/moon toggle; SDK-54 dep alignment + Expo entry fixes                                                                                                                                                                                                              |
| #17 | `feat/admin-communities` — Sprint-3 admin slice: admin communities CRUD (`GET/POST /admin/communities`, `GET/PATCH/DELETE /:id`, transactional update/soft-delete) + read-only `GET /admin/reports`; admin web Communities page + Reports queue                                                                                                                                                                   |
| #19 | `feat/posts` — Sprint-4 slice 1: community posts CRUD + report (backend-only, text-only). Cursor-paged feed, member-gated create, block filtering, transactional+audited mutations, DELETE scrubs stored content, defensive cursor, best-effort `new_community_post` push, `idx_posts_community`. 19 integration tests                                                                                            |
| #20 | `fix/db-push-rls-safety` — neutralized `db:push --force` (proposed disabling RLS on every prod table) → safe interactive orchestrator; added `db:rls` + read-only `check:rls` (fails on unlisted public tables); `docs/DEPLOY.md` + incident log. Repaired prod (missing `password_reset_tokens`) + test (RLS disabled on all tables)                                                                             |
| #21 | `chore/autoslice-skill` — `/autoslice` skill: automated Claude↔Codex review loop (plan + working-tree review via the `codex:codex-rescue` subagent, round caps, human gate before PR) + frozen `CODEX_REVIEWER_BRIEF.md`                                                                                                                                                                                          |
| #22 | `feat/moderation-actions` — Sprint-4 admin moderation: `PATCH /admin/reports/:id` (resolve/dismiss, atomic one-way, `AdminReportDTO`) + `POST /admin/moderation/remove-content` (post-only, scrub+audit) + admin reports list → `AdminReportDTO`. 13 integration tests                                                                                                                                            |
| #23 | `feat/user-ban` — Sprint-4 P-15: admin ban/unban + user directory + auth-gate (banned → 403; GDPR export/erasure stay reachable via `isAuthenticatedAllowBanned`) + erasure cascade (clears `bannedAt`, anonymises user-targeted audit `resourceId`). `users.banned_at` additive column. First `/autoslice` run. 27 focused tests. **Prod `users.banned_at` applied + `check:rls` verified (17/17, 0 policies).** |
| #24 | `chore/status-sync-autoslice-tuning` — STATUS sync + `/autoslice` skill tuning: every Codex subagent call must run synchronously and return the verdict in its final message (fixes the Mode-B background-task hiccup)                                                                                                                                                                                            |
| #25 | `feat/posts-feed-mobile` — Sprint-4 mobile: community **Feed** (About\|Feed `SegmentedControl` on the community detail) — cursor-paged `FlatList` + pull-to-refresh + load-more + per-post report; stale-response guard; deleted tombstones. Read-only consumption of #19. 110 client tests                                                                                                                       |
| #26 | `feat/posts-compose-mobile` — Sprint-4 mobile: **composer** (member-gated) + **delete-own** (⋯ → tombstone); race-safe optimistic updates (functional updaters + requestSeq); Profile sun/moon `ThemeToggle`; modal keyboard-avoidance + tap-outside dismiss. 129 client tests                                                                                                                                    |
| #27 | `feat/admin-moderation-web` — Sprint-4 admin-web: Reports queue **actions** wired to #22 — Rozwiąż/Odrzuć (resolve/dismiss) + Usuń treść (remove-content, post-only, confirm); per-row `Set` busy state; reload-reconciles; Polish error banners                                                                                                                                                                  |

**🎉 Sprint-1 auth scope complete** (backend auth #4/#6/#7, mobile auth UI #8, admin sign-in #9). **🎉 Sprint-2 account/GDPR complete** (profile #10, export #11, erasure #12 — P-1/P-2 closed). **🎉 Sprint-3 complete** (communities #13, block/reports #14, theme #15, communities/login mobile #16, admin slice #17). **🎉 Sprint-4 backend complete** (posts #19, db:push/RLS #20, /autoslice #21, moderation #22, user ban/unban #23). **Sprint-4 mobile posts UI** (feed #25, composer #26) + **admin reports-queue actions** (#27) merged.

## In progress

**In flight: `feat/crisis-contacts-backend`** (P-37 safety-page **slice 1 of 3**, backend). A new admin-curated **`crisis_contacts`** table + **public** read API + admin CRUD for the "Pomoc w kryzysie" safety page (`assets/safety-page-*.png`), mirroring the resources/safe-places vertical. Additive `crisis_contacts` table (RLS zero-policy; soft-delete; `created_by_id` set-null on erasure), frozen Article-9-safe `CRISIS_CONTACT_CATEGORIES` (emergency/emotional_crisis/legal/community), `CrisisContactDTO {id,name,phone,description,hours|null,category,verified,createdAt}`. **Reads are PUBLIC** (`GET /api/v1/crisis-contacts (+?category=)` + `/:id`) — safety-critical so crisis help works signed-out — IP-rate-limited (`crisisReadIp`, fail-closed); emergency-first order. **Admin CRUD** (`GET/POST /admin/crisis-contacts`, `PATCH/DELETE /:id`; requireAdmin + `adminMutationUser` + transactional + audited IDs-only). **112 is seeded, admin-editable — never hardcoded.** Supersedes the stubbed `EmergencyContact` type + `/emergency-contacts` row (removed). **Codex Mode A APPROVED (round 2).** **Schema must be applied to TEST+PROD via docs/DEPLOY.md (human) before CI integration passes.** Deferred (P-37): favorites/save, manual ordering (`sortOrder`), url field, outage-resilient caching for the public read, admin-web CRUD page (slice 2), mobile safety page + global Lifebuoy icon (slice 3). The entries below are the recent-merge log.

- **Merged (#87): `feat/home-cards-glass`** — **premium glass revamp of the Home content cards** (client-only UI; no backend/data change). **`CommunityRailCard`** → a landscape **264×160** glass tile (dimensions exported as `RAIL_CARD_WIDTH/HEIGHT/RADIUS` and imported by `RailSkeleton` + Home's dashed add-card so they can't drift): a full-bleed image + SVG bottom gradient, or a code-only placeholder (SVG indigo→violet gradient + radial corner glow + faint glass tint + large initial) + a translucent member-count pill + a hairline top highlight + name/lavender-count at the bottom. It's a **self-contained colored surface → identical in light + dark** (per "dark and light should be the same"). **`EventCard`** → radius 24 + only the **surface branches by mode**: dark = `rgba(30,25,70,0.55)` glass + `rgba(255,255,255,0.14)` border + `rgba(255,255,255,0.22)` inner top-highlight + a softer shadow; light keeps the white card (a dark-glass card can't sit on a white page). Shared component → the revamp also lifts the **Events feed + Saved** in dark. **`CategoryChip`** dark soft-pill nudged to `rgba(124,92,255,0.18)` / `rgba(167,139,250,0.28)` / `#A78BFA` (shared → all category chips in dark). Gradients via `react-native-svg` (existing dep). Tests run the dark path (mock `mode:"dark"`) + stay green. Gate: full client battery + **⚠️ iOS device test (light+dark)** across the rail (image + placeholder tiles, pill, skeleton, add-card), event cards (Home/feed/Saved), and every category-chip surface. Out of scope: the page background, data/layout.

- **Merged (#86): `feat/chatthread-fullbleed`** — the **final screen of the full-bleed header sweep** (client-only UI). Converts `ChatThreadScreen` from the native stack header to full-bleed with the shared `ScreenHeader`: both `ChatThread` registrations (Events + Chat stacks) → `headerShown:false`; the screen renders `ScreenHeader title={communityName} onBack={navigation.goBack}` above the inverted `FlatList` inside the `KeyboardAvoidingView` (which now owns the screen from the top → `keyboardVerticalOffset` changed from `insets.top + 44` to `0`); the `FlatList` gets an explicit `flex:1` so it fills between the header and the pinned composer; the header shows in the loading (skeleton) + error branches too; the dead `setOptions({title})` effect + narrow nav type removed (nav prop widened to `{ goBack }`). Test: the ChatThread suite gains a header-renders-name + back-press→goBack assertion (nav mocks → `{ goBack }`). Gate: full client battery + **⚠️ iOS device test** (light+dark) — the critical checks: keyboard pushes the composer above it while the header stays fixed; the inverted list scrolls; back works from thread/loading/error; opened from **both** a community (Events) and the inbox (Chat). Completes the sweep started in #85.

- **Merged (#85): `feat/fullbleed-headers`** — **app-wide full-bleed header sweep** (client-only UI; no backend). Removes the native React Navigation stack header bar (the solid top "banner", disliked in dark mode) from the remaining pushed screens and gives each a shared, full-bleed custom header. New **`client/components/ScreenHeader.tsx`** — a fixed top row (safe-area top padding + a bordered circular `CaretLeft` back button + optional left-aligned title), mirroring the ResourceDetail/SafePlaceDetail floating-back pattern. Converted (`headerShown:false` in `AppTabs` + render `ScreenHeader`): **Saved** ("Zapisane"), **CreateEvent**, **CreateCommunity**, **CommunityDetail** (back-only — the name/avatar are already in-content; the dead `setOptions({title})` effect removed), **BlockedUsers**, **About** (5 of these ignored safe-area before — the header now owns the top inset; loading/error branches render the back button too). **ChatThread deliberately split to an immediate follow-up slice** (its KeyboardAvoidingView offset / inverted list / pinned composer / dual stack registration are higher-risk — Codex + I agreed). New `ScreenHeader` test + the 6 screen suites updated (goBack mocks + a header title/back assertion). Gate: full client battery + **⚠️ iOS device test** (light+dark): every converted screen's back button + safe-area under the notch; the two forms' keyboard/scroll. Out of scope: the already-full-bleed tab roots; the now-dead stack `headerStyle`/`headerTintColor` options (harmless).
- **Merged (#84): `fix/session-refresh-offline`** — **P-10a: the transient-network logout fix** (client-only, no backend change). `refreshSession()` (`client/lib/session.ts`) collapsed a **network throw** into `"failed"`, and both callers treat `"failed"` as a forced logout — so a momentary connectivity blip during a token refresh signed a valid user out ("Twoja sesja wygasła"). Fix: a new `"offline"` `RefreshOutcome` for a **network throw / 5xx** (auth state unknown) that **keeps the session** — `loadSession()` returns the stored session at cold-start, and the mid-session 401 interceptor (`client/lib/api/http.ts`) doesn't log out (the 401 falls through to `mapError`). Only a **genuine rejection** (no token / 401 / 4xx → `"failed"`) or a **ban** (403 `account_suspended` → `"suspended"`) signs out; the next successful refresh re-checks the ban/deleted gates. Extends the client jest suites (`session.refresh` offline+5xx; `session` cold-start offline-keeps-session; `http.refresh` offline no-logout). Gate: full client battery; **device test optional** (non-visual auth logic with deterministic unit coverage). Deferred (P-10 follow-ups): suspension screen directly on cold-start (b), pair with P-8 revocation (c).
- **Merged (#83): `feat/mobile-resources`** — the **mobile Resources feature** (P-37 Support & Education, **slice 3**). **Reworked after the on-device UX review (2026-07-06)** into **ONE full-bleed directory screen** (was a hub→list two-surface split that tested as confusing — "clicking just takes me to the same page filtered"). Now: `ResourcesScreen` owns its header + an **inline search box** (one tap) + a **category chip row** (filters in place, active chip auto-scrolled into view) + a "Polecane" featured section (default view only) + the list, with a **dim + spinner on filter/search change**; `ResourceDetailScreen` is **full-bleed** with its own floating back button (no native "Materiały" top bar — an iPhone request). `ResourcesListScreen` was **deleted**. **Server-side search added** (was client-side-over-loaded-pages, a crisis-lookup risk): `GET /api/v1/resources` gains **`?search=`** (case-insensitive substring over title+body, `likeEscape`-d, combines with `category`, blank→400) — `resourcesListQuerySchema` + `storage.listResources` (mirrors safe-places) + both routes + `docs/API.md` + 5 integration cases. `useResources` search is now server-side. **Queued follow-ups:** the **P-10a random-logout fix** (`refreshSession()` treats a transient network throw as "failed"→forced logout — own slice next) + an **app-wide full-bleed-header sweep** (Events/Chat/Profile stacks). Original slice-3 notes below still apply for the data layer / icons / palette / safety (card tap always opens detail, never a direct external jump; quick-exit ⚡ not built, P-17). The **dedicated 5th "Wsparcie" bottom tab** (Home · Events · **Wsparcie** · Chat · Profile) stays — an explicit product decision (the mockup's own bar is illustrative). Unchanged data layer: `useResources`/`useResource` (now server-search), `client/lib/api/resources.ts` + `resourcesApiErrorMessage`, `ResourceCard`, `RESOURCE_CATEGORY_ICON_PATHS` + `ResourceCategoryIcon` + `BookOpen`, `RESOURCE_CATEGORY_COLORS` (mockup palette, matched to the admin portal), `strings.resources.*` + `strings.tabs.resources`. Tests: resources api (query composition incl. `search` + status mapping), `useResources` (server search) / `useResource`, the directory + detail screens + `ResourceCard` (card-tap-never-`openURL` guard), i18n category guard, + backend search integration cases (CI). **Deferred (P-37):** resource images/org logos (icon placeholders now), suggest-a-resource → moderation, emergency/crisis contacts. Gate: full client battery + **⚠️ iOS device re-test** (light+dark; the reworked one-screen directory + full-bleed detail + the 5-tab bar) before PR; Android deferred.
- **Merged (#82): `feat/admin-required-asterisks`** — **required-field asterisks across the admin portal** (frontend-only, admin-web; a visual/consistency polish — no backend/schema/API/contract change). The shared `Field` (`admin/src/components/ui.tsx`) gains a `required?` prop that appends a red `*`, plus a small exported `RequiredMark` (decorative — `aria-hidden` + `title="Pole wymagane"`) reused by the raw-`bq-label` category pickers so markup doesn't drift; one `.bq-required` CSS class (`color: var(--danger)`). Mandatory fields marked (verified against `server/validation.ts`): **Login** (E-mail, Hasło), **Communities** (Nazwa), **Safe Places** (Nazwa + the Kategoria picker), **Resources** (Tytuł + Kategoria + Treść). Every "(opcjonalnie)" field stays unmarked; the OSM-search Miasto input is excluded (placeholder-only, no visible label). No input-level `aria-required` (JS-validated, owner-only tool — AR-1). Gate: `cd admin && npm run build` (admin excluded from root `check:types` + not built in CI) + **⚠️ human BROWSER test** across the four forms (red `*` on required only, no layout shift).
- **Merged (#81): `feat/admin-resources-web`** — **admin-web Resources CRUD page** (P-37 Support & Education, **slice 2**; frontend-only, consumes the slice-1 backend #77). A new `admin/src/pages/ResourcesPage.tsx` mirroring `SafePlacesPage` (stripped to resources' shape — no image/coords/OSM/accessibility): list via `DataTable` (Tytuł · Kategoria colour-chip · Wyróżniony badge · Link icon · Edytuj/Usuń), a **category `Select` filter** (backend list is `?category=`-only — **no search box**, matching the contract), `Pagination` + a monotonic `reqSeq` load guard, a create/edit `Drawer` (Tytuł ≤200 · Kategoria chip-picker · Treść Textarea ≤5000 · Link optional url · Wyróżniony checkbox; client-side required checks → friendly `Alert`; PATCH sends the full set so `url:""`→`null` clears + never an empty-body 400), and soft-delete via `useConfirm`. `admin/src/lib/types.ts` gains `RESOURCE_CATEGORIES` + `ResourceCategory` + `RESOURCE_CATEGORY_META` (Polish label + chip colour) + `ResourceDTO` (local mirror — admin has no `@shared`); `Icon.tsx` gains a `book` glyph; `App.tsx` gets a "Materiały" nav item + `/resources` route under **Treści**. **No backend/schema/`docs/API.md` change** (the contract landed in slice 1). Gate: `cd admin && npm run build` green (admin excluded from root `check:types` + not built in CI — **infra follow-up flagged**) + **⚠️ human BROWSER test before PR** (create each category ±URL ±featured; list chip/badge/link; edit + clear-URL→removed; category filter; delete confirm; empty state; 400/429 error copy). Client wants **premium admin UI** (P-40 / [[ui-must-be-impeccable]]).
- **Merged (#67): `feat/safe-place-accessibility`** — **admin-verified venue accessibility features** (epic **P-40**; the "Wheelchair accessible · Gender-neutral restroom · Free Wi-Fi" rows on `assets/event-safeplace-screen.png`). **Data model = admin-verified, CONFIRMED-PRESENT-ONLY** (the team curates it, same as the vetting standard; **never fabricated** — a false "accessible" would send an at-risk user to a place they can't use). Additive **`safe_places.accessibility_features text[] NOT NULL DEFAULT '{}'`** (first array column; applied TEST+PROD; no RLS/table change, **check:rls 19**). Frozen **`ACCESSIBILITY_FEATURES = {wheelchair_accessible, gender_neutral_restroom, free_wifi}`** in `shared/types.ts` (🔒 venue attributes, Article-9-safe, never identity; client extends). `create/updateSafePlaceSchema.accessibilityFeatures` (z.enum array, out-of-set → 400; deduped in storage; update = **full replace**, `[]` clears, omit = unchanged). `SafePlaceDTO.accessibilityFeatures` — both `toSafePlaceDTO` builders **defensively narrow** via `isAccessibilityFeature` so no legacy/unknown value leaks. **Admin web:** a multi-select "Udogodnienia" toggle-chip row on `SafePlacesPage`. **Mobile:** an "Udogodnienia" section on `SafePlaceDetailScreen` (a ✓ + label per **present** feature; the whole section **omitted when empty** — absence = unknown, never "not accessible"). Tests: `safePlaces.integration` (create stores+dedup; update replace/omit-unchanged/[]-clears; out-of-set → 400; **DTO narrows an unknown stored value out**) + client (`SafePlaceDetailScreen` renders present features / section-absent-when-empty; fixtures) + admin `npm run build`. **Deferred (P-40):** OSM pre-fill (map OSM `wheelchair=`/`internet_access=wlan`/`toilets:*` tags → admin-confirmed suggestions), tri-state "known-absent" display, card chips, the extended flag list (client decision — trivial via the frozen tuple). Gate: `cd admin && npm run build` + **⚠️ human BROWSER test** (toggle chips save/persist) + **iOS device test** (the Udogodnienia rows, light+dark).
- **Merged since:** the **mobile Safe Place DETAIL screen** (#66; per `assets/event-safeplace-screen.png`; epic **P-40**). Tapping a `SafePlaceCard` (feed or Saved tab) opens `SafePlaceDetailScreen` — a full-bleed banner (the admin photo or a brand `LinearGradient` placeholder), a floating back + ⋯ button, then the name, category chip, address/city and description, with a pinned **Save** button (reuses the optimistic toggle). **⋯ → Report** goes through a **dedicated** `POST /api/v1/safe-places/:id/report` (mirrors events/posts: parseId→400, `checkReportRateLimit`→429, reason-only `postReportSchema`→400, `getSafePlace` visibility→404, `submitReport({resourceType:"safe_place"})`→201 with a `report.submitted` IDs-only audit) — the generic `POST /reports` enum stays unchanged, so the F-02 no-existence-check gap isn't extended to safe places. `ReportResourceType` gains `safe_place`. **New client:** `getSafePlace(id)` + `reportSafePlace(id,reason)` api, `useSafePlace` hook (mirrors `useEvent`: load/error/retry + refetch-on-focus, optimistic `toggleSave` + revert + per-flight guard, `report`), `safePlacesApiErrorMessage` mapper; `SafePlaceCard` gains `onPress` (card body navigates; the bookmark is a nested Pressable); nav route `SafePlaceDetail:{id}`. **COORDINATES NEVER SHOWN.** Tests: `safePlaces.integration` report route (201 + `safe_place` row + IDs-only audit; 400 bad-uuid/empty-reason; 404 missing/deleted; 429) + client (api getSafePlace/reportSafePlace; `useSafePlace` load/error/retry/toggleSave/report; `SafePlaceDetailScreen` banner-vs-placeholder/name/category/description/no-coords/Save/⋯→Report; card onPress). **Deferred (P-40):** accessibility features (own slice), distance + Directions (SP-4), opening hours, reviews/stars (client decision), Share (universal links), mobile banner upload (SP-6b). Gate: **⚠️ iOS device test** (tap-through, banner/placeholder, Save, Report, light+dark); Android deferred.
- **Merged since:** **SP-6a: venue images (admin-uploaded)** (#65; epic **P-40**; the first end-to-end R2 image pipeline). **Backend:** additive **`safe_places.image_key`** (text, nullable — server-internal, applied TEST+PROD; no RLS/table change, check:rls stays 19); `objectStorage.ts` gains a **`safeplace`** asset type + bucket (`R2_BUCKET_SAFE_PLACE_IMAGES`) and **asset-scoped upload claims** (`upload-claim:{assetType}:{key}`); **SW-1 fixed** — `createUploadUrl(assetType,userId,contentType)` validates the type against an allowlist `{jpeg,png,webp}` and **signs it into** the PUT, and `confirmUpload(assetType,key,userId)` HEADs the stored object → rejects + **deletes** if it's not an allowlisted image ≤ 5 MB. `POST /admin/safe-places/upload-url {contentType}` (`requireAdmin`, `adminMutation`-limited → 429, bad type → 400) → `{uploadUrl,key}`. `create/updateSafePlace` accept `imageKey` (confirmed BEFORE the DB write → false → 400); update: **omit = unchanged · null = remove · uuid = set**. `SafePlaceDTO.imageUrl` = a signed 1 h GET (or null); `toSafePlaceDTO` is now **async** (`Promise.all` over rows in both the user + admin routes); the raw `image_key` is **never serialised**. **Admin web:** an image upload field on `SafePlacesPage` (pick → upload-url → PUT to R2 → preview + remove; list thumbnail). **Mobile:** `SafePlaceCard` shows a **square thumbnail** (per `assets/safe-places-with-map.png`) from `imageUrl`, else a branded placeholder (display-only). Tests: `objectStorage.integration` 8/8 (allowlist reject, HEAD >5 MB / non-image → delete+false, claim-once) + `safePlaces.integration` 38/38 (upload-url 200/403/400/429; create/update with confirmed key → stored + signed `imageUrl`, key never leaked; unconfirmed → 400; set/replace/clear; null image → no signing) + client card image/placeholder + admin `npm run build`. **Deferred (P-40):** mobile-side upload (**SP-6b**, expo-image-picker + dev-client), community/avatar/post images, old-object cleanup on replace/delete, server-side resize, the detail page. Gate: `cd admin && npm run build` + **⚠️ human BROWSER test** (upload/preview/remove/thumbnail) + **iOS device test** (thumbnail/placeholder, light+dark). Needs the **EU R2 bucket** provisioned for the live upload test (R2 mocked in CI).
- **Merged since:** **Save/bookmark safe places + a 2-tab "Zapisane" screen** (#64; epic **P-40 save slice**; mirrors the events `event_saves` feature). **Backend:** additive **`safe_place_saves`** table (private per-user bookmark, both FKs cascade; applied TEST+PROD, in `supabase/rls.sql`, **check:rls 19/19**), `POST`/`DELETE /safe-places/:id/save` (parseId→400, `checkRsvpRateLimit`→429, idempotent, visible-gated→404, **not audited**) + `GET /safe-places/saved` (caller-scoped, cap 50, excludes soft-deleted), `SafePlaceDTO.saved` (private boolean — no count/who-saved). 🔒 **Fixed an always-false bug**: the join-less `listSafePlaces`/`getSafePlace` rendered `${safePlaces.id}` UNqualified inside the EXISTS subquery, colliding with `safe_place_saves.id` → `saved` was always false; now qualified `"safe_places"."id"` (events' reads join, so they were unaffected). 🔒 **GDPR (found + fixed here):** a real erasure anonymises the user row **in place**, so the FK cascade never fired — `event_saves`/`safe_place_saves` **survived erasure**; `eraseUser` now deletes both, tested via the real erasure path. **Art. 20:** the account export now includes `savedEvents` + `savedSafePlaces` (`{id, title|name, savedAt}`). **Client:** interactive **Bookmark on `SafePlaceCard`** (optimistic `useSafePlaces.toggleSave`, revert-on-failure) + `useSavedSafePlaces` (refetch-on-focus, stale-guard, optimistic remove-on-unsave) + `SavedScreen` (renamed from `SavedEventsScreen`; `SegmentedControl` [Wydarzenia, Bezpieczne miejsca], default Events) hosting `SavedEventsList` + `SavedSafePlacesList`; nav route `SavedEvents`→`Saved` (reuses the `Bookmark` glyph) — i18n `saved.*` + `safePlaces.save*`. Tests: safePlaces integration 33/33 (save/unsave/saved/404/429-each-verb/no-audit/erasure-both-tables/export) + client (api, toggleSave optimistic+revert, useSavedSafePlaces, card bookmark, SavedScreen 2-tab, SavedEventsList). Gate: **⚠️ iOS device test** (light+dark); Android deferred (store gate).
- **Earlier:** **Safe Places "Import from OSM"** (P-40 SP-2, #62 — Overpass search → curated bulk-add, `osm_id` dedupe, admin panel; map-free). **Map-free** (visual map deferred to SP-4). Additive **`safe_places.osm_id`** (text) + a **partial unique index** `idx_safe_places_osm_id` (WHERE osm_id IS NOT NULL) — applied TEST+PROD, check:rls 18/18; server-internal (NOT on `SafePlaceDTO`). New `server/overpass.ts` (Overpass client: category→OSM-tag map, city-area query, User-Agent + 15s timeout, parses named/coord candidates, `OverpassError`). Admin endpoints: `POST /admin/safe-places/osm-search` (city+category → candidates; Overpass down → 502; **no user PII** leaves us, raw body never logged) + `POST /admin/safe-places/bulk` (curated array ≤100 → `storage.bulkCreateSafePlaces`: app-dedupe within-request + DB `onConflictDoNothing` on the partial index; transactional + per-row `safe_place.created` audit IDs-only → `{created, skipped}`). Admin UI: an **"Importuj z OpenStreetMap"** panel on SafePlacesPage — city + category → results **checklist** (editable category per row, "Zaznacz wszystkie") → "Dodaj zaznaczone" → shows created/skipped + refreshes. Tests: `overpass.integration.test.ts` (parsing/error mapping, **fetch mocked**) + safe-places suite gains osm-search (200/502/403/400/429) + bulk (create/dedupe/within-request-dup/validation/403/429/audit-IDs-only). Overpass = a new sub-processor (COMPLIANCE). Gate: `cd admin && npm run build` + **human browser test**. **Deferred (P-40):** mobile list/map (SP-3/4), user submissions (SP-5), photos (SP-6).
- **Earlier:** **Safe Places admin CRUD page** (P-40 SP-1, #61 — frontend-only, consumes #60). Replaces the `admin/` stub with a polished CRUD page: list (`GET /api/admin/safe-places` + category dropdown + city search + pager), a create/edit form (name, a **colour-chip category picker**, description, address, city, **optional lat/lng** — client validation mirrors the server: name+category required, coords **both-or-neither** + range), and soft-delete (confirm → `DELETE /:id`). Category display chips (Polish label + colour) in the table + a "✓ na mapie / — brak" coords indicator. `admin/src/lib/types.ts` gains `SafePlaceDTO` + `SAFE_PLACE_CATEGORIES` + `SAFE_PLACE_CATEGORY_META` (local mirror; admin has no `@shared`). **Deferred (P-40):** geocode helper (Nominatim) + the OSM "browse nearby → bulk-add" map panel (SP-2), mobile list/map (SP-3/4), user submissions (SP-5), real photos (SP-6). **No backend change.** Gate: `cd admin && npm run build` green (admin isn't in CI — infra follow-up flagged); **⚠️ human BROWSER test before PR** (list/filters/create/edit/validation/delete). Client wants **premium admin UI** for this feature (P-40).
- **Merged since:** **Safe Places BACKEND** (Sprint 7 slice 1 — read API + admin CRUD + `%3F` coord-leak fix; no new table/column, check:rls 18/18 TEST+PROD; #60). Frozen `SAFE_PLACE_CATEGORIES` (coarse venue-type, Article-9-safe) narrows `SafePlaceDTO.category`. **User read API** (🔑): `GET /api/v1/safe-places` (offset + `?category=&city=&near=lat,lng`; deterministic order `city,name,id`; `near` → null-coords-last then nearest-first great-circle, **ephemeral** — order-by only, never persisted/logged) + `GET /safe-places/:id`. **Admin CRUD** (`requireAdmin`, `adminMutationUser`-limited, transactional + audited **IDs-only**): `GET/POST /admin/safe-places` + `PATCH/DELETE /admin/safe-places/:id` (soft-delete). lat/lng **both-or-neither** on create+update. 🔒 **Privacy fix (found by Codex):** the `%3F` URL-fix middleware logged 60 chars of the decoded URL — it now logs the **path only**, so an encoded `?near=lat,lng` (or a reset token) can't leak into logs; extracted to `server/resendUrlFix.ts` + a regression test. Tests: `safePlaces.integration.test.ts` (list/filters/near-order/null-last/pagination-stable/invalid-near→400/excludes-deleted/unauth; :id 200/404/bad-uuid; admin create/update/delete incl. 403/400/429 + audit-exact-shape) + `resendUrlFix.integration.test.ts`. **Deferred (tracked):** mobile **map + list UI** (needs the R2/PMTiles basemap + MapLibre dev-client) + **admin-web CRUD page** (now SP-1, this branch).
- **Earlier:** event **categories MOBILE UI** (slice D2 — reusable `CategoryChip` feed filter chips + create picker + card/detail chip; `useEvents` hook-owned `?category=` filter + `toggleSave`; nav fix (Events-tab reset + `initial:false`); premium `EventCard` redesign (white/soft-glass `card` token, dividers, metadata icons, Polish `goingLabel` plural, uniform height, save bookmark); CreateEventScreen card-section redesign; **closes epic D**, #59); event **categories backend** (slice D1 — **no backend/schema change**. A reusable **`CategoryChip`** ({label, selected?, onPress?}) used three ways: **feed filter chips** (a horizontal row under the search box — "Wszystkie" + the 8 categories → `useEvents` gains a hook-owned `category`/`setCategory` that refetches server-side via `?category=`, race-safe on the existing `requestSeq` guard + a synchronous `categoryRef`), a **create-form picker** (optional toggle chips → `createEvent({ category? })`), and a **read-only display chip** on `EventCard` + `EventDetailScreen`. `listEvents(cursor?, category?)` composes both query params via `encodeURIComponent`. Polish labels: `strings.events.categories` (8) + `filterAll`/`categoryLabel`/`emptyCategory`. Empty-copy precedence: search-empty → `emptySearch`; else category-empty → `emptyCategory`; else `empty`. Tests (client jest): api query composition + create payload; `useEvents` setCategory/reset/loadMore-consistency/clear; `CategoryChip`; card/detail chip presence; filter-chip tap→setCategory + "Wszystkie" reset; create picker inclusion/omission; i18n label-for-every-key. **Search stays client-side** (server search P-28); **edit-category UI deferred** with the edit/delete-event UI. **Device-test polish (round 1):** (1) **nav fix** — tapping the Events tab now always lands on the events LIST (a `tabPress` reset), and Home-opened events keep the list beneath (`initial:false`) so Back works — previously the Events tab could get stuck on a lone EventDetail; (2) **filter chips were stretched into full-height capsules** (a horizontal `ScrollView` filling the column) → constrained with `flexGrow:0` + the list takes `flex:1`, which also un-collapsed the list so filtering visibly works; (3) **category chips modernised** — Phosphor per-category glyphs (`CategoryIcon`) + a soft brand-tint selected state (feed filter, create picker, card, detail); (4) **CreateEventScreen redesigned** to the `events-page-details.jpeg` card-section layout (icon-badge headers per field; `TextField` gained `hideLabel`). **Round 2:** the **feed EventCard was restyled** to the `events-screen.png` stack — title, a Clock **time** row, a MapPin **location** row, the going **count**, then the **category tag** — plus a tappable **save/bookmark** in the card corner (`useEvents.toggleSave(id)`: per-id-guarded optimistic flip + revert-on-failure; the event stays in the feed and (dis)appears on the Saved screen). `EventCard` gained an optional `onToggleSave` (feed passes it; the Home rail / saved list omit it → no bookmark). **⚠️ DEVICE TEST before PR (human): iOS light+dark** — Events tab always → list; feed filter chips are compact + switch the list; category shows on card + detail; the create page card layout + category picker; category-empty copy. No rebuild (JS-only).
- **Merged since:** event **categories backend** (slice D1 — additive `events.category` + `idx_events_category_start` applied TEST+PROD, `EVENT_CATEGORIES` frozen taxonomy + `z.enum`, `EventDTO.category`, `GET /events?category=` via a dedicated `eventsListQuerySchema`, Article-9-safe event-type-only, #58); event **Save/bookmark mobile UI** (slice C2 — two-button "Pójdę + Zapisz" bar, filled `Bookmark` when `event.saved`, optimistic `toggleSave` with synchronous guards + finally-reset, `SavedEventsScreen` + `useSavedEvents`, device-test polish: squircle+Check, instant RSVP, fixed-width Save, animated ⋯ drawer, #57); events **Save/bookmark backend** (slice C1 — `event_saves` table applied TEST+PROD, `POST`/`DELETE /events/:id/save` + `GET /events/saved`, `EventDTO.saved`, private/no-who-saved, #56); **P-39 hardening batch** — IV-1 (query `safeParse`→400), TXN-1 (join/leave/block/unblock audit writes wrapped in `db.transaction`), INJ-02 (`likeEscape` on search), SEC-LOG-01 (`safeErrorCode` extraction + firebase name:message) — closing the audit's actionable-now items (#55); **rate-limit sweep** — fail-closed limiters on the six previously-unguarded mutations (RSVP + event update + post/message/event delete + community leave), 429 regression test each (audit RL-1/2/3, #54); **security fix (AUTH-1)** — Realtime chat ban/erasure bypass closed by the `banned_at/deleted_at` guard in `chat_topic_is_member` (applied to TEST+PROD; predicate regression-tested; residual instant-kill tracked P-8, #53); events **cancel + past/expired mobile UI** (slice B2 — Event Detail cancelled/past notice + disabled RSVP bar + creator ⋯ cancel action gated by `canCancel`, `useEvent.cancel()` + cancel error mapper, backend past-cancel→409, #52); events **cancel + past/expired backend** (slice B1 — `POST /events/:id/cancel` creator-only, additive `status`/`cancelledAt` applied to TEST+PROD, `EventDTO` `status`/`cancelledAt`/`past`/`canCancel`, feeds exclude cancelled, race-safe `setRsvp`→409, #51); admin-web **event removal** in the reports queue (type-aware "Usuń wydarzenie/treść", #49); event **⋯ overflow + Report event** (mobile Event Detail action sheet → reused `ReportPostModal` → `POST /events/:id/report`; SHARE deferred to a universal-links slice, #50); Home **upcoming-events rail** (caller's going events via `GET /events/mine` + silent refetch-on-focus, #48); events **Detail visual redesign** (immersive banner + date badge + binary "Pójdę" toggle, slice A, #47); events **create-event UI** (member-only form on Community detail + native date/time pickers, slice 3, #46); events **mobile UI** (feed + detail + RSVP, slice 2, #45); events **backend** (slice 1 — events API + RSVP + moderation hooks, `goingCount` aggregate only, `reminder_sent_at` additive DDL on prod+test, P-13/Sprint-6, #44); community chat **Messages inbox** (Chat tab → ChatStack, `GET /chats`, P-24b, #40/#41) + inbox polish; community chat **mobile thread + Realtime auth** (P-24a, #39); community chat **backend** — messages API + `server/realtime.ts` (#38); content-moderation-at-scale doc (P-38, #43) + pitch-deck reconciliation (#42); Home shell + app-wide dark-mode UI pass (Phosphor tab icons P-11, `ScreenBackground` gradient, `react-native-svg`) (#37); cold-start refresh (P-10, #36); mobile session-token refresh (P-10 mid-session, #35); mobile suspension UX / login gating (P-20, #31); moderator delete-others in the feed (#32); admin users/ban-unban UI (#29); DPIA docs + processor entity (Ladly Media FZ-LLC) + Polish translation (#33/#34).
- Then deferred Sprint-4 bits: emergency contacts (content-source decision pending), the P-19 polish, P-20 follow-ups (export/delete CTAs, deterministic cold-start probe), and Android device test of the posts UI.
- **PAUSED (P-17):** quick-exit + discreet-mode safety UI — kept un-wired (`App.tsx` note) pending a client/product safety decision (a visible trigger may shame users / be a "tell").
- **Pending device testing:** Android pass for the mobile posts UI (#25/#26) — iOS Expo done; Android deferred by the maintainer.
- **Backlog (Codex):** "Deactivate account" = a **reversible pause** (not GDPR erasure) — parked in [ROADMAP](ROADMAP.md) **Sprint 4**.

## Auth endpoints live (`/api/v1/auth/*`)

`signup` · `resend-verification` · `login` · `google` · `forgot-password` · `reset-password`. (All merged. `google` live flow still needs the Supabase Google-provider dashboard step before a real device can use it.) **No regular-user `GET /me`/`/account` endpoint yet** (P-1) — the mobile app persists the profile from the auth response.
Account (🔑): `GET/PATCH /api/v1/profile` · `POST /api/v1/account/change-password` · `GET /api/v1/account/consents` (merged #10) · `GET /api/v1/account/export` (merged #11) · `DELETE /api/v1/account` (merged #12). **Account/GDPR surface complete.**
Communities (🔑, merged #13): `POST /api/v1/communities` · `GET /api/v1/communities` · `GET /:id` · `POST /:id/join` · `DELETE /:id/leave`.
Posts (🔑, merged #19): `GET /api/v1/communities/:id/posts` · `POST /api/v1/communities/:id/posts` · `GET /api/v1/posts/:id` · `DELETE /api/v1/posts/:id` · `POST /api/v1/posts/:id/report`.
Events (🔑, merged #44): `GET /api/v1/events` · `GET /api/v1/events/mine` (Home rail — caller's going events, `feat/home-upcoming-events`) · `POST /api/v1/communities/:id/events` · `GET /api/v1/events/:id` · `PATCH /api/v1/events/:id` · `DELETE /api/v1/events/:id` · `POST /api/v1/events/:id/rsvp` · `POST /api/v1/events/:id/report`. (`goingCount` aggregate only — no attendee identities; RSVP member-gated.) Mobile feed/detail/RSVP #45, create #46, detail redesign #47.
Safety (🔑, merged #14): `POST /api/v1/blocks` · `DELETE /api/v1/blocks/:userId` · `GET /api/v1/blocks` · `POST /api/v1/reports`.
Admin (🛡️ `isAuthenticated → requireAdmin`): **`POST /api/admin/login`** (#9, merged) + `GET /api/admin/me`. **`feat/admin-communities`:** `GET/POST /api/admin/communities` · `GET/PATCH/DELETE /api/admin/communities/:id` · `GET /api/admin/reports` (read-only). **`feat/moderation-actions`:** `PATCH /api/admin/reports/:id` (resolve/dismiss) · `POST /api/admin/moderation/remove-content` (posts + events; backend #44, admin-web event button `feat/admin-event-removal`). **`feat/user-ban`:** `GET /api/admin/users` · `GET /api/admin/users/:id` · `POST /api/admin/moderation/ban` · `POST /api/admin/moderation/unban`. Paths stay under `/api/admin/*` (→ `/api/v1/admin/*` migration tracked).

## Sprint status

Sprint 1 (auth) ✅ · Sprint 2 (account/GDPR, P-1/P-2 closed) ✅ · Sprint 3 (communities → block/reports → mobile → admin) ✅ · Sprint 4 (posts → moderation → user ban) ✅ · Sprint 5 (community chat: backend #38 → thread/Realtime #39 → inbox #40/#41; DMs paused per pitch-deck, P-26) ✅ · **Sprint 6 (events)** ✅ (backend #44 → feed/detail/RSVP #45 → create #46 → detail redesign #47 → Home rail #48 → ⋯/Report #50 → cancel/past #51/#52 → save #56/#57 → categories #58/#59) — **⚠️ except notifications** (only the send pipeline was built; push-token route + prefs screen + all-push-types + reminder/retention cron **still open** — see ROADMAP Sprint 6 correction + Known follow-ups). **Now in Sprint 7** (Safe Places **P-40 epic**: backend #60 → admin CRUD #61 → OSM import #62 → mobile list/search #63 → save #64 → images #65 → detail #66 → accessibility #67 → **UI redesign #68**; open: map/near-me SP-4, submissions SP-5 — plus the newly-scheduled deck-pillar content: Support/Education **P-37**, News **P-31**, Initiatives **P-32**, profile depth **P-33**) per [ROADMAP](ROADMAP.md).

## Infrastructure

| Service                                                                      | Status                                                                     |
| ---------------------------------------------------------------------------- | -------------------------------------------------------------------------- |
| Supabase **prod** (`blis-q-prod`, Frankfurt)                                 | live — 17 tables, RLS-on, Data API off                                     |
| Supabase **test** (`blis-q-test`, Frankfurt)                                 | live — CI integration DB; `BLISQ_TEST_*` secrets set                       |
| Firebase (`Blis-Q`)                                                          | live — Google provider + service account (server creds in `.env`)          |
| Upstash Redis / Cloudflare R2 / Resend domain / Fly.io / Sentry / RevenueCat | **not yet** (deferred; Fly/Sentry later, rest at the provisioning cutover) |

All infra is under the `blisqadmin@gmail.com` project account (PGC-owned) — **transfer ownership to the client before real users** (see ROADMAP Week-0 + the provisioning-cutover plan).

## Known follow-ups (CLAUDE.md issue tracker)

- **P-1/P-2** ✅ **closed** (before real users): GDPR registration consent (#4), profile/`GET me` (#10), export (#11, Art. 20), erasure (#12, Art. 17). The account is fully manageable, exportable, and erasable.
- **P-3** (🟠 blocker, before payments): RevenueCat webhook.
- **P-6**: branded Resend email (currently Supabase built-in).
- **P-7**: Drizzle `pgTable` extra-config deprecation sweep.
- **P-8** (before beta): force-logout other sessions on password reset.
- **P-9**: ✅ addressed in this branch (reset deep-link token captured + scrubbed from nav/web state, never logged); re-verify when universal/App Links land.
- **P-10**: ✅ mobile token refresh wired — mid-session (#35) + cold-start (#36); the stored refresh token is exchanged via `POST /api/v1/auth/refresh`. Non-blocking follow-ups remain (transient-offline cold-start outcome, cold-start suspension surface, P-8 pairing).
- **Notifications (Sprint-6 gap, before beta):** only the `server/notifications.ts` **send pipeline** exists (Expo push + prefs check + stale-token cleanup), wired best-effort into new posts + new events. **Open:** ⚠️ `POST/DELETE /api/push-tokens` register/deregister route (client `usePushNotifications.ts` has nowhere to POST → **no device receives a push today**); ⚠️ `GET/PATCH /api/notification-preferences` + a mobile prefs screen; ⚠️ the remaining push types (`event_reminder`, `community_invite`, `new_member_joined`, `moderation_action`); ⚠️ the **`event_reminder` + retention cron** (retention is a **compliance** gate — COMPLIANCE §5.4). Scheduled into Sprint 7 / a "notifications delivery" slice — see [ROADMAP](ROADMAP.md) Sprint 6 correction.
- **Deck-pillar content (scheduled 2026-07-05):** the deck's pillar-3 **Support & Education** (P-37), pillar-4 **News** (P-31) + **Initiatives/surveys/statistics/volunteering** (P-32), and pillar-1 **profile/networking depth** (P-33) were tracker-only; now folded into ROADMAP Sprint 7. See the [ROADMAP](ROADMAP.md) "Pitch-deck pillar coverage" table.

## ⛔ Remaining blockers (must clear before beta / real users / launch)

Tracked here so they stay explicit (per Codex), not just implied by the roadmap.

**Code / engineering (PGC):**

- 🔴 **GDPR erasure + export** (P-1/P-2) — export `GET /api/account/export` ✅ merged (#11); erasure `DELETE /api/account` cascade **in review** (this branch). Once merged, P-1/P-2 close. **Hard gate before any beta testers** (testers are real users).
- 🟡 **Reset-session revocation** (P-8) — password reset doesn't force-logout other Supabase sessions. Before beta.
- ✅ **Mobile token refresh** (P-10) — wired: mid-session (#35) + cold-start (#36). Non-blocking follow-ups remain (transient-offline outcome, cold-start suspension surface, P-8 pairing).
- 🟠 **RevenueCat webhook** (P-3) — before any payments (Sprint 8).
- 🟡 **Branded Resend email** (P-6) + 🟢 Drizzle extra-config sweep (P-7).

**Mobile provisioning / device testing** (not needed until the first EAS/device build — I'll flag when): Supabase Google provider, `EXPO_PUBLIC_GOOGLE_*` client IDs, app.json `iosUrlScheme`, iOS Associated Domains / Android App Links, EAS dev client; real iOS + Android device testing each sprint.

**Legal / infra gates (🏢 Client + provisioning)** — see [ROADMAP](ROADMAP.md) Week-0 + critical path: ⛔ **DPA signed**, ⛔ **DPIA complete** (schema can't be finalised until then), **Privacy Policy + ToS live** (PL), and the remaining infra cutover (Upstash Redis, Cloudflare R2, Resend domain, Fly.io, Sentry — all **not yet** provisioned).

## Next decision

**Sprint 5 chat underway.** Backend (#38) merged. `feat/community-chat-thread` (P-24a: mobile thread + Realtime auth) implemented → Codex Mode B → battery → human gate (the live-Supabase auth spike + on-device test + `realtime-auth.sql` deploy per `docs/DEPLOY.md`) before PR. Next: the **Messages inbox / Chat-tab root** (P-24b, from `chat-screen.png`), then rich chat features (P-24c), then **1:1 DMs** as a dedicated safety/DPIA-gated slice (P-26). Mobile-provisioning (EAS dev client, Supabase Google provider, etc.) is needed for live device testing of chat.
