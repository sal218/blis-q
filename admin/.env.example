# Base URL of the Blis-Q Express API.
# In development, Vite proxies /api to http://localhost:5000 (see
# vite.config.ts), so this can be left blank. In production, set it to the
# deployed Fly.io API origin (e.g. https://blis-q.fly.dev).
VITE_API_URL=
