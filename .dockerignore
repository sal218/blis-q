# The server image needs only package manifests, tsconfig, and the server +
# shared source (the Dockerfile COPYs those explicitly). Everything else is
# excluded to keep the build context small and the image server-only.
node_modules
.git
.github
.agents
admin
client
assets
dist
static-build
.expo
drizzle
*.md
.env
.env.*
!.env.example
npm-debug.log*
.DS_Store
